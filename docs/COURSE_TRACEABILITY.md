# Portfolio Management course traceability

This is the permanent mapping from audited course evidence to PortfolioLens. Course files were not modified or copied. A matching name is never treated as methodological equivalence without formula, assumption, code, and test evidence.

| Exact course source and section | Concept and PortfolioLens status | Formula or rule | Assumptions | Code and tests | Limitations and documentation |
|---|---|---|---|---|---|
| `2026S_FIN5745 PM_Workbook 1. Risk & Return of Portfolio Investments_Q.xlsx`; risk/return exercises | Arithmetic expected return, variance, covariance, portfolio moments — **Completed, Phase 1** | `E[r_p]=w′μ`; `Var(r_p)=w′Σw`; annualization `252×mean`, `252×sample variance` | Daily simple returns; constant weights; complete aligned observations | `performance.py`; formula, matrix, label, and reconciliation tests in `test_analytics.py` | Historical estimates are not forecasts; see `METHODOLOGY.md` Performance |
| `2026S_FIN5745 PM_Workbook 2. MW Efficient Frontier & Capital Market Line_Q.xlsx` and `_A.xlsx`; Sharpe/Markowitz/CML sections | Shared Sharpe, GMV, long-only frontier, target-return portfolios, constrained tangency, non-leveraged CAL — **Completed, Phases 1/2B** | minimize `w′Σw`; target `w′μ=μ*`; maximize `(w′μ-r_f)/sqrt(w′Σw)`; CAL `r_f+y(E[r_T]-r_f)`, `yσ_T` | `0≤w≤1`; sum one; `0≤y≤1`; arithmetic sample means/covariance; no borrowing | `construction.py`, `performance.py`, `app.py`; bounds, sum, feasibility, GMV, Sharpe, monotonicity, reproducibility, CAL, and failure tests | Sample-sensitive; no shorting/leverage or recommendation claim; see Construction methodology |
| `2026S_FIN5745 PM_Workbook 4. Securities Selection & Single Index Model _Q.xlsx`; single-index model | Excess-return OLS, alpha, beta, R², residual/systematic/idiosyncratic risk — **Completed, Phase 2A** | `r_p-r_f=α+β(r_m-r_f)+ε`; `β=Cov/Var`; `R²=1-SSE/SST`; systematic variance `β²Var(m)` | Aligned daily excess returns; annual risk-free divided by 252; one benchmark factor | `risk.py`, `pipeline.py`; known-coefficient, risk-decomposition, validation, and integration tests | Benchmark/sample dependent; not causality or skill evidence; see Risk and benchmark methodology |
| `2026S_FIN5745 PM_Workbook 5-1. CAPM, APT & Multifactor Models_Q.xlsx`; CAPM section | CAPM required return and Jensen’s alpha — **Completed, Phase 2A** | `r_f+β(E[r_m]-r_f)`; Jensen `E[r_p]-required return` | Arithmetic annualized returns; same aligned sample and beta | `risk.py`; CAPM/Jensen/regression-alpha reconciliation tests | Expected market return is historical sample estimate; APT/multifactor live features excluded |
| `2026S_FIN5745 PM_Workbook 7. Evaluation of Portfolio Performance_Q.xlsx`; performance evaluation | Jensen, Treynor, active return, tracking error, information ratio — **Completed, Phases 2A/2D** | Treynor `(E[r_p]-r_f)/β`; active `252×mean(r_p-r_b)`; TE `std(r_p-r_b)√252`; IR `active/TE` | Daily aligned portfolio/benchmark returns; beta must be nonzero for Treynor | `risk.py`, `formatting.py`, `app.py`; synthetic formula and active-return/IR tests | Cumulative excess return is separately labeled; rolling alpha/beta not audit-approved |
| `2026S_FIN5745 PM_Workbook 6. Portfolio Management Strategies_Q.xlsx`; strategy/rebalancing sections | Buy-and-hold, monthly/quarterly/annual/threshold rebalancing, drift, turnover, costs — **Completed, Phase 2C** | drift from holdings; one-way turnover `0.5Σ|trade|/V`; cost `rate×Σ|trade|` | Returns occur before close-of-period trade; fractional holdings; no cash flows/taxes/market impact | `rebalancing.py`, `app.py`, `reporting.py`; no-trade, schedules, triggers, turnover, cost, continuity, drift, reconciliation tests | Simulation differs intentionally from daily constant-weight analytics; see Rebalancing methodology |
| `PF_IPS_CFA_Final.docx`, `investment policy statement_individual investors_CFA-1.pdf`, `investment policy statement_institutional investors_CFA-1.pdf`; constraints/monitoring | Asset bands, exclusions, explicit category caps — **Completed, Phase 2E** | `min_i≤w_i≤max_i`; exclusions `max_i=0`; group `Σw_i≤cap`; sum one | User supplies every group label and cap; long-only; no silent relaxation | `construction.py`, `app.py`, `reporting.py`; band, exclusion, group, feasibility, parser, and breach-summary tests | No inferred classifications; maximum-volatility constraint not approved; see Construction methodology |
| Workbook 2 mean-variance comparison sections | Like-for-like supported portfolio comparison — **Completed, Phase 3** | Existing performance formulas applied to each weight vector; distance `0.5Σ|w_s-w_c|` | Same return sample, risk-free rate, and constant-weight model | `research.py`, `app.py`, `reporting.py`; comparison reconciliation tests | Weight distance is not realized turnover |
| Course-supported sensitivity concepts; PortfolioLens synthesis | Explicit long-only weights and holding shocks — **Completed, Phase 3** | scenario performance via existing formulas; instantaneous impact `Σw_is_i` | Nonnegative weights sum one; one finite explicit shock per holding | `research.py`, `stress.py`, `app.py`; validation and shock reconciliation tests | Not a forecast, rebalance simulation, or implicit asset-class scenario |
| Application-specific synthesis; not a course formula | Transparent Portfolio Health Score — **Completed, Phase 3; not course-derived** | disclosed weighted/clipped components and available-weight rescaling | Historical Sharpe, drawdown, CVaR, diversification, IR; coverage shown | `research.py`; arithmetic, bounds, and missing-coverage tests | Heuristic only; not suitability, optimality, or advice; see Research workspace methodology |
| Workbook 1/2/4/5-1 concepts | Two-asset, frontier/CAL, CAPM and single-index intuition — **Educational-only, Phase 4** | Worked formulas in companion | Deterministic examples; no external data | `docs/education/PORTFOLIO_MANAGEMENT_COMPANION.md` | Explanatory material, not separate production models |
| Workbook 5-1 and `Assignment/treynor_black_portfolio_model.py`; advanced-model sections | Treynor–Black, APT, multifactor overview — **Educational-only, Phase 4** | Conceptual factor/active-model forms only | No approved factor data or alpha-forecast process | Educational companion; no runtime implementation | Intentionally excluded from live app due data/estimation scope |

## Portfolio Management Workbook 1 — Risk & Return of Portfolio Investments

Source inspected directly: `2026S_FIN5745 PM_Workbook 1. Risk & Return of Portfolio Investments_Q.xlsx`. The source is outside this repository and is never read by the deployed application. All nine visible nonempty worksheets were inspected, including formulas, drawings, charts, workbook names, hidden state, and package metadata. No material hidden rows, columns, or sheets were found. No active Excel table, data-validation rule, or recoverable Solver model exists. Thousands of broad names, eight external links, Capital IQ/`@RISK` labels, and broken references are template residue rather than active methodology.

### Worksheet inventory

| Worksheet | Purpose and active inputs | Calculated outputs and formulas | Objects / exercises | Classification and limitations |
|---|---|---|---|---|
| `Pf Concepts (H)` | Handwritten concept-map submission; instructor drawings define beginning/end price, dividend, scenario probabilities, returns, weights, variances, covariance and correlation | HPR `(P1-P0+D)/P0`; arithmetic/probability mean `ΣpR`; geometric growth `terminal^(1/n)-1`; population variance `Σp(R-E[R])²`; `CV=σ/E[R]`; `Cov=ρσσ`; two- and three-asset variance; covariance-matrix `w′Σw` | No worksheet formulas or charts; one student-submitted image plus instructor formula drawings; handwritten/photo requirement | Graded deliverable plus instructional reference. Student handwriting is not authoritative and was not copied. |
| `Coin-toss` | Two equiprobable two-flip games, $100 start; Game 1 outcomes `40,10,10,-20`, Game 2 `70,10,10,-50` | `SUMPRODUCT(p,r)=10` for both; `STDEV.P` gives `21.213` and `42.426`; expected range `E±σ` | Risk-aversion/rational-choice questions; no chart | Educational probability example. Percent payoffs are added, not sequentially compounded. Population risk is appropriate to the complete outcome set. |
| `Risk & Return w P` | Four-state mutual-fund example with NAV 100, year-end price, dividend, probabilities and 4% T-bill; separate five-year HPR sample | HPR; probability expected return `9.7625%`; population variance `0.0379734`; SD `19.4868%`; risk premium `HPR-rf`; expected premium `5.7625%`; CV `1.9961`; five-year arithmetic `2.098%`, `VAR.P`, `STDEV.P`, geometric `0.54385%` | Formula-display cells; no chart | Instructional/practice. Annual HPRs are simple returns. T-bill is a same-horizon annual risk-free convention; no benchmark. |
| `Correlation` | Five historical annual observations for assets A/B/C and equal 50:50 A/B and A/C portfolios | Arithmetic means; `STDEV.P`; `CORREL`; weighted return; two-asset variance `wA²σA²+wB²σB²+2wAwBσAσBρ`; SD; unlabeled `σp/E[rp]` CV | Two return-pair scatterplots and diversification questions | Historical classroom example using population risk; no annualization beyond already-annual observations. |
| `Covariance` | Twelve monthly 2016 stock- and bond-index returns entered in percentage-point units | Means, deviations, squared deviations, cross-products; `VARP`; `STDEV.P`; `COVARIANCE.P`; `ρ=Cov/(σiσj)` | Monthly-series line chart and stock/bond scatterplot | Historical monthly population convention; values remain monthly percentage points and squared percentage points. No annualization. |
| `Pf Diversification` | Three probability states for Auto and Gold; saved weights `61.9508%/38.0492%` | Probability expected returns; population variances/SDs; covariance `-129.35`; correlation `-0.99459`; scenario portfolio return; `w′Σw` equivalent variance and SD | Questions ask 50:50 results, lowest-risk weight, and correlation; no chart | An optimization objective is implied by the question and saved hardcoded weight, but no Solver metadata or explicit bounds survive. Exact Solver constraints cannot be claimed safely. |
| `Limit of Diversification` | Equal-weight asset counts, common asset SD 50%, common correlations 0 and 0.4 | `σp=sqrt[(1/n)σ²+((n-1)/n)ρσ²]`; convergence to zero at `ρ=0` and to `σsqrt(ρ)` at positive common correlation | Two scatterplots and interpretation prompt | Instructional common-correlation model; illustrates diminishing diversifiable risk and a non-diversifiable floor, not an empirical estimator. |
| `EX. Risk & Return` | Five annual returns for stocks T/B | Arithmetic means `5.4%/1.6%`; `VAR.P`; SD `11.4647%/5.6780%`; CV `2.1231/3.5488`; geometric means `4.757%/1.435%` | Written comparison questions and student analysis; no chart | Practice/graded exercise using simple annual returns and population risk. |
| `EX. Corr & Covar` | Five annual percentage-point observations for Lauren/Kendall | Means; population variances/SDs; `COVARIANCE.P=-4.64`; `CORREL=-0.06604`; deviations and cross-products | Written diversification interpretation; no chart | Practice/graded exercise. One covariance formula centers only one series, algebraically equivalent because the other series' centered deviations sum to zero; not a separate method. |

### Concept-to-code traceability

| Worksheet / concept | Workbook methodology | PortfolioLens implementation and status | Methodological difference / action | Tests and documentation |
|---|---|---|---|---|
| Concepts; `Risk & Return w P` — HPR | `(P1-P0+D)/P0`, simple return | `performance.holding_period_return` — **fully implemented** | Live adjusted prices already embed distributions; UI warns against adding income twice | `test_workbook_one_holding_period_arithmetic_geometric_and_annualization`; `METHODOLOGY.md` |
| Multiple sheets — arithmetic mean / expected return | `ΣR/n` or `ΣpR`; finite examples | Periodic arithmetic helper, asset table, annualized `252×mean`, `w′μ` — **fully implemented** | Historical mean is an estimate, not a forecast; probability tables are not inferred from market history | Workbook 1 return tests; performance and matrix tests |
| Concepts / exercises — geometric mean | `[Π(1+R)]^(1/n)-1` | Periodic geometric helper and asset table; CAGR annualizes the same path — **fully implemented** | Explicit periodic geometric mean and annualized CAGR labels prevent conflation | Compounding/annualization test; Methodology section |
| All risk exercises — variance / SD | Probability population moments or `VAR.P`/`STDEV.P` | Asset table and scorecard use annualized sample variance/SD — **implemented differently** | Intentional: finite outcome universe versus sample estimate of unknown market distribution; UI labels `Sample` | Sample-risk and unit reconciliation tests; Decision D022 |
| `Covariance`; `Correlation` — covariance / correlation | Population covariance and correlation; monthly or annual units unchanged | Daily sample covariance ×252 and daily sample correlation matrices — **implemented differently** | Annualization and `n-1` are explicit; correlation is unitless and not annualized | Covariance/correlation/two-asset test; Methodology section |
| Concepts; `Correlation`; `Pf Diversification` — portfolio return/variance/SD | Weighted scenario return; two-asset expansion; multiasset `w′Σw` | Constant-weight `w′μ`, `w′Σw`, square-root volatility — **fully implemented** | PortfolioLens uses aligned historical sample inputs and annual units | Existing matrix tests plus Workbook 1 two-asset reconciliation |
| Concepts / exercises — coefficient of variation | `CV=σ/E[R]` | Asset table uses annualized sample volatility divided by annualized arithmetic return — **fully implemented** | Same horizon in numerator/denominator; undefined at zero mean; negative-mean interpretation limited | Asset table and zero-denominator tests; Methodology section |
| `Correlation`; `Pf Diversification`; `Limit of Diversification` — diversification | Lower portfolio SD from less-than-perfect correlation; common-correlation limiting formula | Observed weighted standalone volatility, portfolio volatility, absolute/relative reduction — **fully implemented for practical historical workflow** | Does not call the gap systematic risk and does not forecast a common correlation | Diversification reconciliation test; UI caption and Methodology |
| Concepts — covariance matrix / multiasset mathematics | Border-multiplied covariance matrix and expanded three-asset formula | Matrix portfolio variance and annualized covariance display — **fully implemented** | Matrix implementation replaces manual cell multiplication without copying formulas | Existing matrix/weight-label tests |
| `Correlation` — weighted portfolio return | Per-observation `Σw_iR_i` | Constant-weight daily `portfolio_returns` — **fully implemented** | Missing observations rejected after common-date alignment | Portfolio return and missing-data tests |
| Concepts — contribution to return / variance | Matrix cells imply pairwise variance terms; no standalone contribution report | Total-return contribution and Euler volatility contribution — **implemented differently / PortfolioLens extension** | Existing contributions reconcile to total return and volatility; they are not mislabeled as workbook formulas | Existing attribution tests; core Methodology |
| `Coin-toss`; `Risk & Return w P` — probability/scenario tables and risk aversion | User-specified finite probabilities and outcomes | **educational-only** | A generic probability editor would duplicate stress/what-if concepts and weaken focused market-research workflow | Documented here; no runtime code |
| `Limit of Diversification` — identical-risk/common-correlation limit | Closed-form equal-weight limiting model | **educational-only** | Useful intuition but artificial assumptions are not reliable live portfolio inputs | Documented here; no runtime code |
| `Pf Diversification` — implied minimum-risk search | Saved hardcoded weights; no recoverable Solver configuration | Tested long-only GMV optimizer already exists — **implemented differently** | Workbook bounds/constraints cannot be reconstructed; PortfolioLens explicitly requires `0≤w≤1`, sum one and validates convergence | Construction optimizer tests and methodology |
| `Risk & Return w P` — risk premium | Asset/scenario HPR minus same-horizon T-bill | Portfolio/benchmark excess returns, Sharpe and CAPM already exist — **implemented differently** | PortfolioLens converts annual risk-free input consistently for daily analyses; no asset-level scenario risk-premium table added | Sharpe/CAPM tests and benchmark Methodology |
| Sharpe, downside risk, benchmark analytics | Not substantively present in this workbook | Existing PortfolioLens features — **not workbook-derived** | No Workbook 1 implementation claim | Traced to other sources elsewhere in this document |

Every substantive worksheet is therefore implemented, explicitly implemented under a justified historical-sample convention, or recorded as educational-only. No workbook formula, macro, student answer, stale external link, or template plugin residue was copied into runtime code.

## Portfolio Management Workbook 2 — Mean-Variance Efficient Frontier & Capital Market Line

Source inspected directly: `2026S_FIN5745 PM_Workbook 2. MW Efficient Frontier & Capital Market Line_A.xlsx`. It is a read-only external course source and is not used at runtime. All seven visible nonempty worksheets were inspected, including formulas, cached outputs, drawings, images, charts, names, external relationships, freeze panes, and hidden state. No hidden sheets, rows, or columns materially affect the model. The `.xlsx` contains no VBA project. It has no active Excel tables or data validation. Seven external links, approximately 2,400 Capital IQ/`@RISK` and template names, broken references, and the instructor's absolute save path are residue; only the worksheet-local Solver names described below are active methodology.

### Worksheet inventory

| Worksheet | Purpose, inputs, formulas and outputs | Objects / ownership | Status and limitations |
|---|---|---|---|
| `Pf Concepts (H)` | Handwritten concept-map requirement. Instructor drawings state Markowitz assumptions: one-period expected utility, diminishing marginal utility, variability as risk, mean/variance decision criteria, risk aversion, and efficiency dominance. Additional images teach zero covariance with a risk-free asset, complete-portfolio return/volatility, CAL/CML slope, lending/borrowing, total/systematic/unsystematic risk and diversification limits. | Six images; no cells with calculations, chart, table or Solver. Student handwritten/photo submission area is separate from instructor logic. | Instructional/graded. No numeric utility function, risk-aversion coefficient, indifference-curve model, or optimal-complete-portfolio calculation is supplied. |
| `two Assets Pf with W ` | Two risky assets with expected returns 10/20, variances 49/100, weights and correlations from +1 to −1. `Cov=σiσjρ`; `Var_p=wi²σi²+wj²σj²+2wiwjCov`; `σp=sqrt(Var_p)`; `E[Rp]=wiE[Ri]+wjE[Rj]`. Weight cases remain between 0 and 1. | Seven scatter charts show risk/return curves by correlation and combined cases; four instructional images; freeze at `C6`. | Fully traced two-asset intuition. Inputs are assumed percentage units with no time-series estimation or annualization source. |
| `three Assets Pf with W` | Stock/bond/cash-equivalent expected returns 12/8/4, SDs 20/10/3, weights 60/30/10, correlation matrix, covariance conversion and expanded three-asset variance. Outputs: expected return 10, variance 170.784, SD 13.0684. | One formula image; no chart or Solver. | Fully traced multiasset mathematics. “Cash equivalent” has 3% SD and is therefore not the risk-free asset used in the later CML drawings. |
| `MW Efficient Global Pf` | Eight assumed country-index means/SDs and correlation matrix; covariance `ρijσiσj`; equal-weight `w′Σw`; long-only target-return minimum-volatility portfolios. Saved frontier targets 6–27 with weights and SDs; chart includes assets, equal-weight point, and frontier. | One scatter chart, four images including Solver screenshot; freeze at `A59`. Instructor model/output sections; target weights are Solver outputs. | Solver is recoverable and long-only. Means/SDs are percentage values with no stated period or annualization process. Correlation entry `D22=0.45` is asymmetric versus `G19=0.453855…`; formulas use the full entered matrix, so the source covariance matrix is slightly asymmetric. PortfolioLens sample covariance is symmetric by construction. |
| `MW Optimal Pf & CML` | Seven annual-style excess-return assumptions and covariance matrix. Portfolio risk premium `w′μ_excess`; variance `w′Σw`; SD; Sharpe `risk premium/SD`. Saved unconstrained frontier, minimum-variance point, maximum-Sharpe/tangency point, and excess-return CML `RP=σ×Sharpe_T`. | One two-series scatter chart, five images including maximum-Sharpe and minimum-variance Solver screenshots; freeze at `A3`. | Solver is recoverable and allows negative weights. CML is plotted in risk-premium space with implicit zero intercept; total expected return requires adding `r_f`. No explicit numeric risk-free rate is stored because inputs are already excess returns. |
| `EX. IPS` | Franklin IPS case: objectives, risk tolerance, time horizon, liquidity, taxes, legal/regulatory and unique circumstances; asset-allocation ranges and a proposed target. | No formulas, charts or Solver; freeze at `A15`. Instructor answer/case-study narrative. | Educational-only for PortfolioLens. Existing user-defined bands/groups support transparent constraints, but personalized IPS authoring, tax advice, leverage and inferred classifications remain excluded. |
| `Pf Models` | Conceptual hierarchy: complete portfolio = risk-free + overall risky portfolio; Markowitz risky portfolio; single-index equity portfolio; CAPM/security market line. Identifies changing allocation `y` versus risky weights `w`. | Static comparison table only. | Educational map. It supports two-fund separation intuition but no additional live calculation beyond existing construction, regression and CAPM features. |

### Recoverable Solver configurations

| Worksheet / model | Objective | Changing cells | Constraints | Method and saved evidence |
|---|---|---|---|---|
| `MW Efficient Global Pf` target-return portfolio | Minimize `C75`, portfolio SD | `C63:C70` | Each weight `0≤C63:C70≤1`; `C71=1`; `C76=target mean` (saved names currently hold 15; screenshot illustrates 12) | GRG Nonlinear; precision `0.000001`, convergence `0.0001`, 100 iterations. Saved targets 6–27 were generated through repeated target solves. |
| `MW Optimal Pf & CML` tangency portfolio | Maximize `C28`, Sharpe ratio | `B17:B23` | `B24=1`; nonnegative-variable option is unchecked | GRG Nonlinear; saved weights include negative France and Canada positions, confirming short selling. Two stale duplicate `C26=0.04` constraint names remain in package metadata but are absent from the visible/current Solver screenshot and do not match the saved tangency return; they are not treated as active methodology. |
| `MW Optimal Pf & CML` minimum variance | Minimize `C27`, portfolio SD | `B17:B23` | `B24=1`; nonnegative-variable option unchecked | Recoverable from the dedicated screenshot. It is an unconstrained-short-sales minimum-variance solve. The saved named Solver state reflects the later maximum-Sharpe run, so the screenshot is the authoritative evidence for this alternate objective. |

### Concept-to-PortfolioLens traceability

| Worksheet / concept | Workbook methodology | PortfolioLens equivalent and status | Difference / recommendation | Tests / documentation | Career and interview value |
|---|---|---|---|---|---|
| `two Assets`; `three Assets` — portfolio moments | Assumed means, SDs, correlations; explicit two-/three-asset expansion | `w′μ`, `w′Σw`, covariance/correlation views — **fully implemented** | PortfolioLens uses annualized historical sample estimates rather than assumed percentage inputs | Matrix, two-asset and contribution reconciliation tests; Methodology | Foundational portfolio mathematics and matrix translation |
| `MW Efficient Global Pf` — covariance matrix | `Cov_ij=ρijσiσj`; manually bordered matrix | Daily sample covariance ×252 — **implemented differently** | Source contains a slight asymmetric correlation input; PortfolioLens covariance is symmetric from data | Covariance and matrix tests | Data-quality and model-audit discussion |
| `MW Efficient Global Pf` — target-return optimization | Minimize SD subject to exact target, sum one, `0≤w≤1` | `target_return_weights` — **fully implemented** | PortfolioLens minimizes variance, which has the same minimizer as positive SD; target is an annual arithmetic estimate | Target feasibility/equality/bounds tests | Solver translation and constraint reasoning |
| `MW Efficient Global Pf` — efficient frontier / GMV | Repeated long-only target solves; chart includes assets and equal weight | Long-only upper frontier beginning at GMV — **fully implemented and exposed in Portfolio Optimization** | PortfolioLens automatically finds GMV and generates denser reproducible points; workbook table includes inefficient targets below its observed GMV | GMV, monotonicity, reproducibility and non-socket UI exposure tests | Core portfolio-construction interview value |
| `MW Optimal Pf & CML` — unconstrained GMV | Minimize SD with sum one; negative weights permitted | Long-only GMV — **implemented differently** | Short sales are deliberately excluded from PortfolioLens | Bounds and GMV tests; Decision D023 | Explains constraint impact on feasible sets |
| `MW Optimal Pf & CML` — tangency / maximum Sharpe | Maximize excess-return Sharpe, sum one, negative weights permitted | Long-only constrained maximum Sharpe — **implemented differently and exposed in Portfolio Optimization** | Same objective family, different feasible set and expected-return input convention; no exact-output claim | Sharpe consistency, bounds, synthetic behavior and non-socket UI exposure tests | High interview value: methodology labels do not imply equivalence |
| `MW Optimal Pf & CML`; Concepts — CAL/CML | Risk-premium line `RP=σ×Sharpe_T`; drawings add risk-free intercept and show lending/borrowing | Total-return non-leveraged CAL — **fully implemented for lending segment** | PortfolioLens adds entered annual `r_f`; line stops at `y=1` | CAL endpoint/slope tests; Methodology | Two-fund separation and risk-budget communication |
| Concepts; `Pf Models` — complete portfolio | `E[r_c]=w_rf r_f+(1-w_rf)E[r_M]`; `σ_c=(1-w_rf)σ_M` | User-selected risk-free/tangency complete portfolio — **fully implemented and exposed in Portfolio Optimization** | Uses PortfolioLens long-only tangency and restricts `0≤y≤1`; the UI labels direct allocation as risk preference, not a risk-aversion coefficient | Complete/CAL, zero-risky, negative-excess, leverage-rejection and non-socket UI exposure tests | Practical allocation and formula reconciliation |
| Concepts — borrowing / leveraged CML | `y>1` region illustrated | **educational-only / intentionally excluded** | Conflicts with non-leveraged product boundary and requires financing assumptions | Decision D023 | Useful conceptual contrast, inappropriate default feature |
| Concepts — risk aversion / expected utility | Verbal assumptions only | **educational-only** | No coefficient, quadratic utility, indifference curve or optimal `y` formula can be safely inferred | Documented limitation | Shows discipline against inventing methodology |
| `EX. IPS` — policy objectives/constraints | Personalized narrative and allocation bands | Explicit user-defined asset bands/groups — **partially implemented / differently scoped** | PortfolioLens supports transparent construction constraints, not IPS authoring, taxes, suitability or inferred categories | Constraint feasibility/compliance tests | Relevant to portfolio governance; personalized workflow excluded |
| `Pf Models` — single-index and CAPM hierarchy | Static conceptual mapping | Existing regression and CAPM modules — **fully implemented elsewhere, not derived from this workbook's calculations** | No duplicate code | Workbook 4/5 trace and tests | Connects construction with benchmark analysis |

Every substantive worksheet is accounted for. Course-derived implementation is limited to recoverable formulas and constraints. PortfolioLens choices—long-only historical estimation and no leverage—are explicitly separate. Suggested sensitivity and instability diagnostics remain professional enhancements rather than retroactively attributed course content.

## Final classification

### Completed

- Foundational portfolio moments and consistent Sharpe methodology
- Excess-return regression, CAPM, Jensen, Treynor, active return, tracking error, and information ratio
- Long-only frontier, GMV, constrained tangency, target-return construction, and non-leveraged CAL
- Holdings-level buy-and-hold, periodic and threshold rebalancing with turnover/costs
- Explicit asset bands, exclusions, user-defined group caps, feasibility and compliance validation
- Research comparison, what-if analysis, deterministic insights, Health Score, and professional report

### Deferred

- Strategy subperiod/validation-split diagnostics and engineering refactors are useful but are not remaining approved Portfolio Management requirements.
- Equal-risk-contribution, volatility targeting, Brinson attribution, and fixed-income analytics require additional methodology or data design.

### Educational-only

- Two-asset derivation, frontier/CAL intuition, CAPM interpretation, and Treynor–Black/APT/multifactor overview.

### Intentionally excluded

- Monte Carlo portfolio clouds and course-derived “risk parity,” which the audit did not substantively support.
- Rolling alpha/beta and maximum-volatility constraints, which approved traceability does not require.
- Short selling, leverage/borrowing, inferred classifications, Treynor–Black/APT/multifactor live models, fixed-income instrument workflows, IPS authoring, tax-lot optimization, personalized advice, and LLM-generated investment commentary.
